Original prompt: Implement and prove the next GrokHack P0 production slice: extract player movement and its directly coupled collision, door, terrain, bounds, occupancy, trap-trigger, visibility, turn-cost, and transfer-intent decisions into one deterministic shared reducer used by both the Node origin and Cloudflare Workers shadow runtime; extend the immutable origin journal and ShadowReplay catch-up path so production-shaped movement traces are byte-stable across runtimes without dual authority.

## 2026-07-13 continuation

- Active delivery surface: draft PR #2, branch `codex/movement-shadow-parity`.
- Reproduced blockers: committed preparation is not reconciled on restart; crash-created preparation temp files survive; V1 vitals are applied after envelope append.
- Independent review also found that marker deletion precedes later origin side effects/persistence, so the current marker cannot certify whole-turn durability.
- Current work: implement a bounded durable preparation/outbox recovery boundary without changing live authority or legacy DTOs.
- Implemented locally: fixed/legacy temp recovery, `prepared` versus `origin_applied` marker phases, V1 reducer application before envelope commit, and cleanup only after all synchronous turn effects.
- Explicit non-proof: `origin_applied` does not certify the later debounced DuckDB commit; authority transfer remains gated on a state transaction/outbox.
- Focused Node 22 proof: 2 files / 72 tests pass, including a real child-process crash after fixed-temp fsync, corrupt-temp fencing, applied-phase write failure, V1-before-envelope ordering, and synchronous-effect-before-cleanup ordering.
- Targeted ESLint and the exact 319-diagnostic server baseline pass; `git diff --check` passes.
- A two-file coverage run exercised `origin-journal.ts` at 83.66% statements / 79.28% branches / 95.32% functions / 87.19% lines, but correctly failed repository-wide thresholds because unrelated files were intentionally absent. Full-suite coverage remains required.
- Still required: focused crash/fault matrix, independent re-review, scoped security scan, canonical root/edge/MCP gates, isolated runtime/media QA, exact-state receipt, commit/push, PR promotion assessment.
- Explicit boundary: no deployment, traffic routing, live-data mutation, secret upload, paid resource, irreversible migration, or external load.

## 2026-07-13 hot-path audit

- Reproduced a new preparation-recovery amplification defect: one consuming movement enumerated the entire `movement-turn-v1` directory five times; a second movement repeated all five scans.
- Fixed recovery to probe the one canonical fixed temp path directly on every operation and perform the legacy random-temp compatibility scan once per shared journal state.
- Capped legacy temp cleanup at 64 candidates and fail closed without unlink amplification when that inventory is exceeded.
- Focused proof: the same two-move harness now records one initial compatibility scan and zero scans for the second movement; the four normal/failure/odd regression cases pass under Node 22.
- Reproduced two cold-crash false-clean windows with real child WorldServer processes and isolated DuckDB. The deeper window survived after `origin_applied` marker cleanup but before the legacy 400 ms debounced persistence: the committed envelope described new coordinates/turns, persisted state stayed old, and restart incorrectly reported `degraded=false`/`pending=false`.
- The origin now retains `origin_applied` while committing the player plus every affected source/destination floor in one serialized DuckDB transaction. Pre-commit process death or an injected row failure rolls the whole snapshot back; only a successful `COMMIT` advances durably to `persistence_committed`, after which cleanup may proceed. Floor rows now include traps and both event-state structures. Graceful shutdown joins the in-flight chain and reports durability failure if poison remains.
- Current focused recovery proof is 83/83 across the complete origin-journal and WorldServer movement matrix under Node 22, including two-floor success/failure fencing, same-process applied/persistence-marker acknowledgement recovery, graceful-shutdown success/poison paths, the real before-ack process death, all marker crash boundaries, tamper rejection, and normal/odd movement decisions. The two-process DuckDB integration case has an explicit 60-second test budget; each child still has its own 20-second timeout.
- Reproduced a stale-copier defect: after the Worker had already advanced beyond the first replay page, a valid copier restart at cursor zero rejected the ahead durable checkpoint even though the immutable origin journal contained the exact suffix. Added the Worker's durable `lastEnvelopeHash` acknowledgement and require the copier to prove that hash, both state heads, and terminal bit locally before it may advance past the sent page. The focused copier suite is 14/14 and the real edge ShadowReplay suite is 27/27 under Node 22.
- Reproduced a QA cleanup defect where a timed-out state wait retained its WebSocket listener. Timeout, error, and close now clean up every listener; exact human/agent movement, wait, and inventory assertions pass 17/17 focused QA tests.
- The production-shaped WorldServer-to-journal-to-Worker harness now passes under Node 22 for ordinary movement, bounds, doors, combat intent, bear traps, floor transfer, terminal starvation, append failure, divergence, response loss, 64-entry paging, Worker reload, duplicate replay from cursor zero, and exact envelope/state-head acknowledgements. Its dirty-source receipt is evidence for the current worktree only; a clean exact-commit rerun remains required.
- Independent review caught that strict V1 turn-state continuity rejected legitimate poison/world/monster effects applied after the shared reducer. A focused regression reproduced the false 422. The invalid fence was removed: nested turn entries still validate their hash chain and reducer result, while the documented origin-effects boundary remains open until those reducers are shared. The production-shaped E2E now publishes two consecutive real World turns with surviving poison damage and proves the Worker accepts the authenticated boundary at checkpoint 2.
- Still required: canonical root/edge/MCP gates, finalized scoped security scan, isolated runtime/media QA, immutable exact-head receipt, independent review, push, and PR disposition.
